public enum MessageType {
    CHAT,      // regular broadcast message
    PRIVATE,   // direct/private message
    SYSTEM,    // system notifications like joins/leaves
    COMMAND    // internal/command messages
}
